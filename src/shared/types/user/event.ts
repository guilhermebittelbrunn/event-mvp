export enum EventStatusEnum {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}

export enum EventAccessTypeEnum {
  GUEST = 'guest',
  OWNER = 'owner',
}
